# Firestore via ADC (recommended locally)
# GOOGLE_APPLICATION_CREDENTIALS=C:\\path\\to\\service-account.json

# Firestore via explicit credentials (use either ADC above or the three below)
FIREBASE_PROJECT_ID=
FIREBASE_CLIENT_EMAIL=
# Remember to escape newlines as \n
FIREBASE_PRIVATE_KEY=

# Optional: where to store local JSON fallback when Firestore is not configured
DATA_DIR=.data
